# Extrair os artigos sobre medicamentos no Wikipedia

## Categorias -> Medicamentos -> Correlações = Grafo

```bash
uv add wikipedia-api
```

## Exemplo

```py
import wikipediaapi

# Define o user-agent (boas práticas da API)
wiki_wiki = wikipediaapi.Wikipedia(
    'SeuAppDeExemplo (seuemail@exemplo.com)',
    'pt'
)

page_gato = wiki_wiki.page('Gato')

if page_gato.exists():
    print(f"Título: {page_gato.title}")

    # O texto completo (já limpo)
    # print(f"Resumo: {page_gato.summary}")
    print("\n--- Texto do Artigo (parcial) ---")
    print(page_gato.text[:500] + "...") # Imprime os primeiros 500 caracteres

    # Links
    print("\n--- Links Internos ---")
    # page_gato.links é um dicionário {título: objeto_da_pagina}
    for i, title in enumerate(list(page_gato.links.keys())):
        if i >= 5: # Limita a 5 para o exemplo
            break
        print(title)

    # Imagens (usando action=query por baixo dos panos)
    # (Esta biblioteca específica pode não ter um método direto para 'images',
    # mas a biblioteca 'wikipedia' tem. A 'wikipedia-api' foca mais no texto.)
```
